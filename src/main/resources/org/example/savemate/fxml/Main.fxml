<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.image.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml" fx:controller="org.example.savemate.controller.MainController">
    <top>
        <ToolBar>
            <Label text="SaveMate" style="-fx-font-size: 16px; -fx-font-weight: bold;" />
            <Pane HBox.hgrow="ALWAYS" />
            <Button text="👤" onAction="#handleUserIcon" />
        </ToolBar>
    </top>
    <center>
        <VBox alignment="CENTER" spacing="10">
            <Label fx:id="resumenLabel" text="Resumen financiero del mes:" style="-fx-font-weight: bold;" />
            <Label text="Total Gastado: 120€" />
            <Label text="Total Ingresado: 200€" />
            <Label text="Saldo Actual: 80€" />
            <HBox alignment="CENTER" spacing="10">
                <Button text="Añadir Gasto" onAction="#añadirGasto" />
                <Button text="Añadir Ingreso" onAction="#añadirIngreso" />
            </HBox>
            <HBox alignment="CENTER" spacing="10">
                <Button text="Historial" onAction="#verHistorial" />
                <Button text="Presupuestos" onAction="#verPresupuestos" />
            </HBox>
        </VBox>
    </center>
</BorderPane>
